<%= form_for(@reminder) do |f| %>
  <% if @reminder.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@reminder.errors.count, "error") %> prohibited this reminder from being saved:</h2>

      <ul>
      <% @reminder.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :title %><br>
    <%= f.text_field :title%>
  </div>
  <div class="field">
    <%= f.label :time_from_now %><br>
    <%= f.text_field :time_from_now %>
  </div>
  <div class="field">
    <%= f.label :send_to %><br>
    <%= f.text_area :send_to %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
